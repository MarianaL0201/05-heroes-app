# Heroes APP

## Levantar desarrollo

1. Clonar el repositorio
2. editar el archivo `.env` con las viarbles de entorno basado ene el archivo `.env.template`
3. Ejecutar `npm install`
4. Ejecutar `npm run dev`
